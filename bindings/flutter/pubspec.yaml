name: fero_flutter
description: Flutter bindings for Fero native core
version: 0.1.0
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
      ios:
